// Replace 'yourdomain' with your actual Hostinger domain
const config = {
  API_BASE_URL: 'https://skylarkcb.com/backend/api',
  // For local testing, use this instead:
  // API_BASE_URL: 'http://localhost/banking-app/backend/api'
};

export default config;
